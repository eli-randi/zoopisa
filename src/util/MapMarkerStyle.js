const K_WIDTH = 30;
const K_HEIGHT = 15;

const MapMarkerStyle = {
  position: 'absolute',
  width: K_WIDTH,
  height: K_HEIGHT,
  left: -K_WIDTH / 2,
  top: -K_HEIGHT / 2,

  border: '2px solid #676560',
  borderRadius: K_HEIGHT,
  backgroundColor: 'white',
  textAlign: 'center',
  color: '#676560',
  fontSize: 12,
  fontWeight: 'bold',
  padding: 3
};

export {MapMarkerStyle};